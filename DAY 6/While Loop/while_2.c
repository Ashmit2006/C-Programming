#include <stdio.h>

int main()
{
    int i = 1; // Initialization

    while (i <= 10)
    {                      // Condition
        printf("%d\n", i); // Print the current value of i
        i++;               // Increment
    }

    return 0;
}
